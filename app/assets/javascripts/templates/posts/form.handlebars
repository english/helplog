<div class="row">
  <div role="main" class="col-lg-6">
    <form class="form-horizontal">
      <fieldset>
        <div class="row">
          <div class="col-lg-offset-1 col-lg-11">
            <legend>{{postAction}} Post</legend>
          </div>
        </div>

        <div class="form-group">
          <label {{bindAttr for="view.titleField.elementId"}} class="col-lg-1 control-label">Title</label>

          <div class="col-lg-11">
            {{view Ember.TextField valueBinding='title' cols="30" viewName="titleField" class="form-control" placeholder="Title"}}
          </div>
        </div>

        <div class="form-group">
          <label {{bindAttr for="view.introField.elementId"}} class="col-lg-1 control-label">Intro</label>

          <div class="col-lg-11">
            {{view Ember.TextArea valueBinding='intro' cols="30" rows="5" viewName="introField" class="form-control" placeholder="Short intro to appear on post listing"}}
          </div>
        </div>

        <div class="form-group">
          <label {{bindAttr for="view.bodyField.elementId"}} class="col-lg-1 control-label">Body</label>
          <div class="col-lg-11">
            {{view Ember.TextArea valueBinding='body' cols="50" rows="30" viewName="bodyField" class="form-control" placeholder="Body"}}
          </div>
        </div>

        <div class="form-group">
          <div class="col-lg-offset-1 col-lg-11">
            <div class="checkbox">
              <label {{bindAttr for="view.publishedField.elementId"}}>Published?</label>
              {{view Ember.Checkbox checkedBinding="published" viewName="publishedField"}}
            </div>
          </div>
        </div>

        <div class="form-group">
          <div class="col-lg-offset-1 col-lg-11">
            <button {{action 'save' model }} type="button" class="btn btn-primary">Save</button>
          </div>
        </div>
      </fieldset>
    </form>
  </div>

  <div class="col-lg-6">
    <h1>{{title}}</h1>
    <div class="intro well well-sm"><p class="text-info">This will only be shown on the posts index page.</p>{{markdown intro}}</div>
    <div class="body">{{markdown body}}</div>
  </div>
</div>
