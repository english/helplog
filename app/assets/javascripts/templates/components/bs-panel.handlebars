<div class="panel panel-default">
  {{#if heading}}
    <div class="panel-heading">{{heading}}</div>
  {{/if}}

  <div {{bind-attr class="bodyClass :panel-body"}}>{{yield}}</div>

  {{#if footer}}
    <div {{bind-attr class="footerClass :panel-footer"}}>{{footer}}</div>
  {{/if}}
</div>
